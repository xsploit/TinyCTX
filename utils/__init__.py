"""
TinyCTX utility package.

This file exists so local imports like ``utils.tool_handler`` resolve to the
repo's utilities instead of an unrelated third-party ``utils`` package that
may be installed in the Python environment.
"""
