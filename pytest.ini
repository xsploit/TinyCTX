# pytest.ini
# Place this file at the root of the tinyctx repo (same level as agent.py).

[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*